# Backend (optional)

The web app runs entirely in React state — this folder is an optional
Node.js + Express stub you can extend if you want to persist data.

```bash
cd backend
npm install
npm start
```

Then point the frontend at `http://localhost:4000` to wire it up.
